#include "symbol.h"
#include "symbol.h"

void read_stdin(T_symbol *result) {

    // alloc
    // call terminate if malloc fails
/*    char *res;

    int c;
    while ( c = ) {
        // add char at end of res
        // realloc if needed
    }

    if (result->attr.data_type == is_int) {
        // check if int
        // result->attr.var->value.n=...
    }
//    else if ...
*/
}

